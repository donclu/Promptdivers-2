---
description: Run the pre-drop orientation protocol (Tier 1/2/3 per context)
---

Follow `${CLAUDE_PLUGIN_ROOT}/protocols/orientation.md`. Pick the tier that matches the situation:

- New agent / first contact with this repo → Tier 3 (full orientation), or see `/onboard`.
- Returning mid-project → Tier 1/2 per the protocol's own gating.

Read `${CLAUDE_PLUGIN_ROOT}/AGENTS.md`, `${CLAUDE_PLUGIN_ROOT}/AGENT_PROFILE.md`, and `PROJECT_LOG.md` (if present) as part of this.
