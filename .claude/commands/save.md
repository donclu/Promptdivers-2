---
description: Update PROJECT_LOG + handoff so a fresh agent/session can continue
---

Append to `PROJECT_LOG.md`: decisions made, bugs fixed, discoveries, patterns to avoid this session.

Update the handoff block (shape in `templates/project-log.template.md`) so a new agent can resume cold.

If objectives were explicit for this session, include a short debrief (PASS/PARTIAL/FAIL per objective) per `protocols/mission-debrief.md` before closing the log entry — see `/debrief` for the full scored version.
