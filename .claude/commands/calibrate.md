---
description: Prime the knowledge/ store before the first live mission
---

Follow `docs/calibration-protocol.md`. This primes `knowledge/` and `experience/` so the Echelon Ladder (`stratagems/support/echelon-ladder.md`) has something to retrieve against — running the ladder on an uncalibrated project just burns rungs for nothing.

Also run the human's voice/creativity calibration (`docs/onboarding-calibration.md`) if this project produces human-facing output.
