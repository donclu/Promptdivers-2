.DS_Store
Thumbs.db
*.swp
*.swo
*~
*.bak
*.orig

# Editor / IDE (keep .cursor/rules/ in repo; ignore local-only cruft)
.idea/
.vscode/*
!.vscode/extensions.json
*.code-workspace

# Local env or secrets if you experiment in a fork
.env
.env.local
.env.*.local

# Optional local scratch (never commit)
scratch/
local/
